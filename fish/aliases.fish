## VIM
alias e "nvim"

## LS
alias ls "exa"
alias l "ls -l"
alias la "ls -la"

## TREE
alias tree "exa --tree"
alias t "tree"

## GIT
alias g "git"
alias gd "g diff"
alias gl "g pull"
alias gst "g status"
alias ga "g add"
alias gaa "g add --all"
alias gc "g commit"
alias gp "g push"

## RM
alias rrm "rm -Rf"

## CP
alias cp "cp -v"
alias ccp "cp -R"

## MV
alias mv "mv -v"

## TMUX
alias tmux "tmux -2"
alias tm "tmux"
alias at "tm attach -t"
alias tmls "tm list-sessions"

## MAKE
alias m "make"
alias mr "m re"

## PYTHON
alias python "python3"
alias p "python"
alias ip "ipython3"

## CHMOD
alias cx "chmod +x"

## GDB
alias gdb "gdb -q"

## DOCKER
alias d "docker"
alias drm "d rm"
alias da "d start"
alias do "d stop"
alias di "d images"
alias dps "d ps"
alias dpsa "dps -a"

## DOCKER-COMPOSE
alias dc "docker-compose"
alias dcu "dc up"
alias dcd "dc down"
alias dcps "dc ps"
alias dpsa "dcps -a"
